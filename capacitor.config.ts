import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'my.usm.cs.zerowaste.speba',
  appName: 'speba',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
